type Group {
  id: Int!
  name: String!
  color: String!
}

type Query {
  groupsByUserId(userId: Int!): [Group!]
}

type Mutation {
  addGroup(name: String!, color: String!): Group
  deleteGroup(name: String!): Group
}
