type GroupName {
  name: String!
}

type Exercise {
  id: Int!
  name: String!
  slug: String!
  group: Group!
}

type Query {
  exercisesByUserId: [Exercise!]!
}

input ExerciseData {
  name: String!
  slug: String!
  groupId: Int
}

input ExerciseDataOptional {
  id: Int!
  name: String
  slug: String
  groupId: Int
}

type DeletedExercise {
  id: Int!
  name: String!
  slug: String!
  groupId: Int
}

type Mutation {
  addExercise(exerciseData: ExerciseData!): Exercise
  updateExercise(fieldsToUpdate: ExerciseDataOptional!): Exercise
  deleteExercise(id: Int!): DeletedExercise
}
