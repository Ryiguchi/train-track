type Schedule {
  id: Int!
  date: String!
  group: Group!
}

type Query {
  scheduleByUserId: [Schedule!]
  isTodaysGroupSet: Boolean
}

input ScheduleInput {
  date: String!
  groupId: Int!
}

input UpdateScheduleInput {
  id: Int!
  groupId: Int!
}

type Mutation {
  addScheduleDay(scheduleData: ScheduleInput!): Schedule
  updateScheduleDay(fieldsToUpdate: UpdateScheduleInput!): Schedule
}
